{
  "name": "awp-graph",
  "version": "1.0.0",
  "description": "AWP Neo4j 代码调用图谱服务",
  "main": "src/build-graph.js",
  "scripts": {
    "build": "node src/build-graph.js",
    "build:dry": "node src/build-graph.js --dry-run",
    "query": "node src/graph-query.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "note": "图谱构建脚本，运行一次写入 Neo4j。不是长期运行的服务，通过 scripts/start.sh graph 触发。"
}
